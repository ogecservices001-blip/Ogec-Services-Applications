org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Le cache Pub (C:) et le projet (D:) sont sur des lecteurs différents ;
# le compilateur incrémental Kotlin plante sur Windows quand il tente de
# calculer un chemin relatif entre deux racines différentes.
kotlin.incremental=false
